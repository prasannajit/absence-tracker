import React from 'react';
import { DataGrid, GridToolbar } from '@material-ui/data-grid';
import { APIResponse } from '../../types';

const Table = ({ data }: { data: APIResponse }) => {
    return (<div>
        Table
    </div>);
};

export default Table;